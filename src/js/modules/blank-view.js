
// kalli.blankView

{
	init() {
		
	},
	dispatch(event) {
		let APP = kalli,
			Self = APP.blankView,
			el;
		// console.log(event);
		switch (event.type) {
			case "add-recent-file":
				break;
		}
	}
}