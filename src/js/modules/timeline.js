
// kalli.timeline

{
	init() {
		
	},
	dispatch(event) {
		let APP = kalli,
			Self = APP.timeline,
			el;
		// console.log(event);
		switch (event.type) {
			case "custom-event":
				break;
		}
	}
}