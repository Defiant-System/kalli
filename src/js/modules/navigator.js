
// kalli.navigator

{
	init() {
		
	},
	dispatch(event) {
		let APP = kalli,
			Self = APP.navigator,
			el;
		// console.log(event);
		switch (event.type) {
			case "custom-event":
				break;
		}
	}
}